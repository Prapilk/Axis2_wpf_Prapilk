<UserControl x:Class="Axis2.WPF.Views.Settings.SettingsItemTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Axis2.WPF.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Checkboxes -->
        <CheckBox Content="Room View" Grid.Row="0" Grid.Column="0" Margin="5"
                  IsChecked="{Binding RoomView, Mode=TwoWay}"
                  VerticalAlignment="Center"/>
        <CheckBox Content="Show Items" Grid.Row="1" Grid.Column="0" Margin="5"
                  IsChecked="{Binding ShowItems, Mode=TwoWay}"
                  VerticalAlignment="Center"/>

        <!-- Item Background Color -->
        <TextBlock Text="Item Background Color:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <Rectangle Grid.Row="2" Grid.Column="1" Margin="5" Width="50" Height="20" Stroke="Black" StrokeThickness="1">
            <Rectangle.Fill>
                <SolidColorBrush Color="{Binding ItemBGColor, Mode=TwoWay}"/>
            </Rectangle.Fill>
        </Rectangle>
        <Button Content="Select Color" Grid.Row="2" Grid.Column="2" Margin="5" Command="{Binding SelectItemBGColorCommand}"/>

        <!-- Reset Button -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Reset Items Settings" Command="{Binding ResetItemsSettingsCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>