<UserControl x:Class="Axis2.WPF.Views.Settings.SettingsFilePathsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Axis2.WPF.Views.Settings"
             xmlns:converters="clr-namespace:Axis2.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Use same path as client checkbox -->
        <CheckBox Content="Use same path as client" Grid.Row="0" Grid.Column="0" Margin="5"
                  IsChecked="{Binding SamePathAsClient, Mode=TwoWay}"
                  VerticalAlignment="Center"/>

        <!-- Default Client Path -->
        <TextBlock Text="Default Client Path:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" IsReadOnly="True"
                 Text="{Binding DefaultClientPath, Mode=OneWay}"/>
        <Button Content="Browse" Grid.Row="1" Grid.Column="2" Margin="5" Command="{Binding BrowseClientPathCommand}"/>

        <!-- Default MUL Path -->
        <TextBlock Text="Default MUL Path:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" IsReadOnly="{Binding SamePathAsClient}"
                 Text="{Binding DefaultMulPath, Mode=OneWay}"/>
        <Button Content="Browse" Grid.Row="2" Grid.Column="2" Margin="5" Command="{Binding BrowseMulPathCommand}"
                IsEnabled="{Binding SamePathAsClient, Converter={StaticResource InverseBooleanConverter}}"/>

        <!-- Reset Button -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Reset Paths Settings" Command="{Binding ResetPathsSettingsCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>