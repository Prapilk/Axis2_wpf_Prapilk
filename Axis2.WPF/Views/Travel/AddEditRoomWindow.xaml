<Window x:Class="Axis2.WPF.Views.Travel.AddEditRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Axis2.WPF.Travel"
        mc:Ignorable="d"
        Title="Add/Edit Room" Height="560" Width="400">
    <Window.Resources>
        <Style x:Key="ValidationErrorStyle" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="New Group:" Margin="5" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" x:Name="NewGroupNameTextBox" Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="1" Grid.Column="0" Content="Group:" Margin="5" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="5" ItemsSource="{Binding RegionGroups}" SelectedItem="{Binding SelectedRegionGroup}" DisplayMemberPath="Name">
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=NewGroupNameTextBox}" Value="">
                            <Setter Property="IsEnabled" Value="True" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>

        <Label Grid.Row="2" Grid.Column="0" Content="Area:" Margin="5" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="5" ItemsSource="{Binding AreasInSelectedGroup}" SelectedItem="{Binding SelectedArea}" DisplayMemberPath="Name">
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=NewGroupNameTextBox}" Value="">
                            <Setter Property="IsEnabled" Value="True" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>

        <Label Grid.Row="3" Grid.Column="0" Content="Name: " Margin="5" />
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding Name, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ValidationErrorStyle}" />

        <Label Grid.Row="4" Grid.Column="0" Content="X:" Margin="5" />
        <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Text="{Binding X}" />

        <Label Grid.Row="5" Grid.Column="0" Content="Y:" Margin="5" />
        <TextBox Grid.Row="5" Grid.Column="1" Margin="5" Text="{Binding Y}" />

        <Label Grid.Row="6" Grid.Column="0" Content="Z:" Margin="5" />
        <TextBox Grid.Row="6" Grid.Column="1" Margin="5" Text="{Binding Z}" />

        <Label Grid.Row="7" Grid.Column="0" Content="Map:" Margin="5" />
        <ComboBox Grid.Row="7" Grid.Column="1" Margin="5" ItemsSource="{Binding AvailableMaps}" SelectedItem="{Binding SelectedMap}" />

        <Label Grid.Row="8" Grid.Column="0" Content="X1:" Margin="5" />
        <TextBox Grid.Row="8" Grid.Column="1" Margin="5" Text="{Binding X1}" IsReadOnly="True" />

        <Label Grid.Row="9" Grid.Column="0" Content="Y1:" Margin="5" />
        <TextBox Grid.Row="9" Grid.Column="1" Margin="5" Text="{Binding Y1}" IsReadOnly="True" />

        <Label Grid.Row="10" Grid.Column="0" Content="X2:" Margin="5" />
        <TextBox Grid.Row="10" Grid.Column="1" Margin="5" Text="{Binding X2}" IsReadOnly="True" />

        <Label Grid.Row="11" Grid.Column="0" Content="Y2:" Margin="5" />
        <TextBox Grid.Row="11" Grid.Column="1" Margin="5" Text="{Binding Y2}" IsReadOnly="True" />

        <Label Grid.Row="12" Grid.Column="0" Content="Width:" Margin="5" />
        <TextBox Grid.Row="12" Grid.Column="1" Margin="5" Text="{Binding Width}" IsReadOnly="True" />

        <Label Grid.Row="13" Grid.Column="0" Content="Height:" Margin="5" />
        <TextBox Grid.Row="13" Grid.Column="1" Margin="5" Text="{Binding Height}" IsReadOnly="True" />

        <StackPanel Grid.Row="14" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,5,-29">
            <Button Content="OK" Width="75" Margin="5" IsDefault="True" Click="Ok_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="IsEnabled" Value="False"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsValid}" Value="True">
                                <Setter Property="IsEnabled" Value="True"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Cancel" Width="75" Margin="5" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>