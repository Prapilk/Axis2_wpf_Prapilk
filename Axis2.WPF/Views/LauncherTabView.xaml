<UserControl x:Class="Axis2.WPF.Views.LauncherTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Axis2.WPF.Views"
             xmlns:viewModels="clr-namespace:Axis2.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Liste des comptes -->
        <GroupBox Header="Accounts" Grid.Column="0" Margin="5">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="New" Margin="2" Command="{Binding NewCommand}"/>
                    <Button Content="Edit" Margin="2" Command="{Binding EditCommand}"/>
                    <Button Content="Delete" Margin="2" Command="{Binding DeleteCommand}"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}" DisplayMemberPath="Name"/>
            </DockPanel>
        </GroupBox>

        <!-- Détails du compte et actions de lancement -->
        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Détails du compte -->
            <GroupBox Header="Account Details" Grid.Row="0">
                <Grid IsEnabled="{Binding IsEditing}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Name:" Grid.Row="0" Grid.Column="0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding SelectedAccount.Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Account:" Grid.Row="1" Grid.Column="0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding SelectedAccount.AccountName, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Password:" Grid.Row="2" Grid.Column="0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding SelectedAccount.Password, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="IP Address:" Grid.Row="3" Grid.Column="0"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding SelectedAccount.IpAddress, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Port:" Grid.Row="4" Grid.Column="0"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Text="{Binding SelectedAccount.Port, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Alternate Client:" Grid.Row="5" Grid.Column="0"/>
                    <DockPanel Grid.Row="5" Grid.Column="1">
                        <Button Content="Browse..." DockPanel.Dock="Right" Command="{Binding BrowseCommand}" Margin="5,0,0,0"/>
                        <TextBox Margin="5,5,0,5" Text="{Binding SelectedAccount.AlternateClientPath, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="1" HorizontalAlignment="Right">
                        <Button Content="Save" Margin="5" Command="{Binding SaveCommand}"/>
                        <Button Content="Cancel" Margin="5" Command="{Binding CancelCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Actions de lancement -->
            <GroupBox Header="Launch" Grid.Row="1">
                <StackPanel VerticalAlignment="Center" IsEnabled="{Binding CanLaunch}">
                    <Button Content="Launch 2D" Margin="5" Command="{Binding Launch2DCommand}"/>
                    <Button Content="Launch 3D" Margin="5" Command="{Binding Launch3DCommand}"/>
                    <Button Content="Launch Alternate" Margin="5" Command="{Binding LaunchAltCommand}"/>
                    <Button Content="Launch Auto" Margin="5" Command="{Binding LaunchAutoCommand}"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>