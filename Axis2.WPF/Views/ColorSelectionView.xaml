<UserControl x:Class="Axis2.WPF.Views.ColorSelectionView"
             Width="800" Height="600"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Axis2.WPF.ViewModels"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Colonne 1: Liste des teintes -->
        <GroupBox Header="Hues" Grid.Column="0">
            <ListBox ItemsSource="{Binding AllHues}" SelectedItem="{Binding SelectedHue}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ColorIndex}" VerticalAlignment="Center" Width="40"/>
                            <ItemsControl ItemsSource="{Binding MiniSpectrum}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Rectangle Width="8" Height="20" Fill="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- Colonne 2: Détails et Prévisualisation -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Header="Spectrum" Grid.Row="0">
                <ItemsControl ItemsSource="{Binding HueSpectrum}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Rectangle Width="24" Height="24" Fill="{Binding}" Margin="1"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <GroupBox Header="Preview" Grid.Row="1" Margin="0,10,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="0">
                        <ComboBox ItemsSource="{Binding PreviewObjectTypes}" 
                                  SelectedItem="{Binding SelectedPreviewObjectType}" 
                                  MinWidth="80" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding PreviewObjectId}" MinWidth="80" Margin="0,0,5,0" VerticalAlignment="Center" ToolTip="Enter ID in decimal (e.g., 16384 for 0x4000) or hex (e.g., 0x3BB3)"/>
                        <Button Content="Preview" Command="{Binding UpdatePreviewCommand}"/>
                    </StackPanel>
                    
                    <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,10,0,0">
                        <Image Source="{Binding PreviewImage}" Stretch="Uniform"/>
                    </Border>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Colonne 3: Boutons OK/Cancel -->
        <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0,0,0">
            <Button Content="OK" IsDefault="True" MinWidth="75" Margin="0,0,0,5" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" IsCancel="True" MinWidth="75" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>