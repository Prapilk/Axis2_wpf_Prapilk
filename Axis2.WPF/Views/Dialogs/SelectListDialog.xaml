<Window x:Class="Axis2.WPF.Views.Dialogs.SelectListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Axis2.WPF.Views.Dialogs"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:SelectListDialogViewModel}"
        Title="{Binding Title}" SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message}" Margin="0,0,0,10"/>
        <ListBox Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" MinWidth="200" MinHeight="100"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" IsDefault="True" Command="{Binding OkCommand}" Click="Ok_Click" MinWidth="75" Margin="0,0,5,0"/>
            <Button Content="Cancel" IsCancel="True" Command="{Binding CancelCommand}" Click="Cancel_Click" MinWidth="75"/>
        </StackPanel>
    </Grid>
</Window>